package ru.est0y.diceChess.services.fen;

import org.springframework.stereotype.Service;

@Service
public class PieceLetterNotation {
 /*   private final Map<String, PieceCreator> pieces;

    public PieceLetterNotation(List<PieceCreator> pieceCreators) {
        this.pieces = pieceCreators.stream()
                .collect(Collectors.toMap(pc -> pc.getClass().getGenericSuperclass().getClass(),pc->pc));
    }

    AbstractPiece fromLetterNotation(String letterNotation) {
        return pieces.get(letterNotation);
    }*/
}
